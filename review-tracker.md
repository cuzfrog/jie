# Review Tracker — jie-platform

> Open implementation-grade gaps. Resolved history lives in `./addrs/`. Resuming tmr; nothing in flight.

## Resolved

- **Group N** closed 2026-06-12. Twelve v1 gaps surfaced by a final review pass; user-decision batch. ADRs: none (none are architecturally significant per the user's "not an ADR" framing — they're spec corrections and CLI behavior tweaks). Spec changes: (1) `10-configuration.md` Config Validation: `defaultProvider` with an unknown value becomes WARN+ignore (init state) instead of hard-fail; field description and Unknown-field-policy prose updated to match; shape errors (e.g. `defaultProvider: 42`) remain hard-fail. (2) `ui/cli.md` `--continue`: dropped the contradictory "globally" clause; CWD-scoped lookup only; on miss, WARN to stderr and proceed as if the flag were not given (non-fatal). (3) `06-agent-model.md` "Startup Pre-Check": error message changed to scenario 6's `No model has been selected, please login and select a default model.`. (4) `review-tracker.md` Resume Plan: scenario list corrected to 4 and 6 only (no scenario 5 in the file; 1/2/3/9 are TUI-dependent; 7 is MCP-dependent / Day 2). (5,6) `ui/cli.md` `--api-key`: rewritten from "runtime override" semantics to "writes to `~/.jie/auth.json` for the resolved provider" semantics — no prefix assumption, no format validation, exactly one key per provider (auth.json is provider-keyed), intended for CI/automated mode. (7) `.jie/` directory creation on settings write: deferred to the implementer (user flagged as impl concern). (8) `06-agent-model.md` AgentSoul / Frontmatter table / Platform Auto-Wiring: `subscribe:` is now optional in agent `.md` frontmatter (was "yes, may be empty"); if absent, the agent's only subscriptions are its auto-subscriptions (`{agent_key}` plus `leader.prompt` for the leader). `minimal-team.md` updated to match (built-in general.md has no `subscribe:` field). (9) `06-agent-model.md` `write_artifact` / `read_artifact`: now specify the `ToolResult.content` (LLM-visible text) and `ToolResult.details` (afterToolCall hook) field mapping. `write_artifact` success: `content` = `Stored artifact at ${key} (${content.length} chars)`, `details` = `{key, created_at}`. `read_artifact` found: `content` = artifact content verbatim, `details` = `{key, content, created_at}`. `read_artifact` missing: `content` = `Artifact not found: ${key}`, `details` = `null`. (10) `00-overview.md` glossary: removed duplicate `notify` Tool row. (11) `00-user-scenarios.md` scenario 7 YAML: `mcp:mock-tool-A*` (2-part) → `mcp:mock-mcp-server:mock-tool-A*` (3-part, matches spec syntax). (12) `06-agent-model.md` Frontmatter table: added `name:` field as the canonical role identifier (overrides filename stem if present; falls back to filename stem if absent). `AgentSoul.role` description updated. `minimal-team.md` updated to mention `name?` in the file listing.
- **Group M** closed 2026-06-12. Spec corrections only (M1–M3). M4–M10 are implementation notes settled by existing spec/ADR prose and need no spec changes. ADRs: none (not architecturally significant). Spec changes: `06-agent-model.md` "Prompt Ingress & Queuing" gains a synthetic-message format table (`[user]: {prompt}` for `leader.prompt`, `[{source} on '{topic}']: {prompt}` for notify path); `notify` paragraph updated to point at the table; "Tool Telemetry" gains a "What `output` serializes" subsection spelling out that the whole Jie `ToolResult` (content + details + terminate) is JSON-serialized, fields are dropped when `undefined`, and pi-agent's wrapped LLM-visible form is distinct from Jie's raw event form; both `prepareArguments` rows (summary table + pi-agent Integration Contract table) updated to `TypeBox.Value.Check(parameters, raw)` with throw-on-false semantics and explicit "no coercion in v1" note. Mirror change in `03-event-system.md` "Tool Telemetry" subsection.
- **Group K** closed 2026-06-11. Spec-skeleton, MVP scope, supervisor removal, built-in `.md` format, storage layering. ADRs: `14-storage-layer-abstractions.md`, `15-platform-entry-function.md`, `16-builtin-minimal-team-as-manifest-files.md`, `17-v1-mvp-scope.md`. Spec changes: new `04-storage.md`; `04-artifact-store.md` → `05-artifact-store.md` (rewritten, domain-only); `05-agent-model.md` → `06-agent-model.md`; `00-overview.md` glossary; `08-memory.md`, `09-deployment.md`, `10-configuration.md`, `12-installation.md`, `ui/tui.md`, `monorepo-structure.md`, `minimal-team.md` all updated for the new entry-function / no-supervisor / built-in-`.md` shape.
- **Group L** closed 2026-06-11. Config format (JSON for config, `.md`+YAML for content per pi convention). Runtime dependencies (`@earendil-works/pi-agent-core@0.79.1`, `@earendil-works/pi-ai@0.79.1`, `typebox@1.1.38`, `yaml@2.9.0`; bun built-ins for the rest; no commander/yargs/lodash). MCP client deferred to Day 2 (added to ADR 17 §"MVP scope statement"). pi-agent 0.79.1 API drift corrected (`BeforeToolCallContext` shape, `BeforeToolCallResult.block/reason`). Spec changes: `mcp.yaml` → `mcp.json` (11 places); new "File-Format Convention" in `10-configuration.md`; new "jie-platform Runtime Dependencies" in `monorepo-structure.md`; ADR 17 §"MVP scope" amended; `10-configuration.md` "MCP Server Configuration" marked Day 2; `09-deployment.md` startup step 5 marked Day 2; `06-agent-model.md` "Tool Telemetry" / "pi-agent Integration Contract" corrected for 0.79.1; `pi-agent-api-reference.md` corrected for 0.79.1. (The "JSON for config" and "runtime deps" items were initially drafted as ADRs 18/19, but those are not architecturally significant and were folded into spec/prose per the user's "not an ADR" feedback.)

## Open

_None — Group M closed. Implementation ready to begin._

## Resume Plan

Implement `packages/jie-platform/` v1 MVP per the plan below, then exercise scenarios 1/4/5/6/7 against the running platform.

1. `monorepo` skeleton (root `package.json` with workspaces, root `tsconfig.json`, `.gitignore`).
2. `packages/jie-platform/storage/{storage,sqlite-storage,init-db,artifact-store,memory-store,index}.ts` (ADR 14).
3. `packages/jie-platform/core/{event-bus,agent-body,agent-soul,tool,event-payload}.ts` + `tool-error.ts`.
4. `packages/jie-platform/tools/{notify,bash,read_file,write_file,web_search,web_fetch,write_artifact,read_artifact,index}.ts`.
5. `packages/jie-platform/team/loader.ts` + `team/minimal/{TEAM,general}.md` (ADR 16).
6. `packages/jie-platform/config/{settings,auth,resolve-team,resolve-model}.ts` (hand-rolled per ADR 19).
7. `packages/jie-platform/start.ts` (ADR 15).
8. `packages/jie-platform/index.ts` (barrel).
9. `packages/jie-cli/index.ts` (the harness): `-p`, `login`, `logout`, `model`, `team`, `--resume`/`--continue`, `--api-key`, `--version`, `--help`. Stub `jie` (no flag) to print "TUI not implemented" and exit 1.
10. `packages/jie-tui/index.ts` (stub throws).
11. `packages/jie-team/package.json` (placeholder).
12. `bun test` setup with co-located `*.test.ts` files.
13. Run user scenarios 4 and 6 against the platform (no TUI, no MCP). Scenarios 1, 2, 3, 9 (TUI-dependent) and scenario 7 (MCP-dependent — Day 2 per ADR 17) are deferred to the post-MVP test plan. (Note: there is no scenario 5 in `00-user-scenarios.md`.)
