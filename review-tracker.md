# Review Tracker — jie-platform

> Working list of open implementation-grade gaps. ADRs in `./addrs/` are the source of truth for consequential decisions. Detail (full edit trail, recommendations, sub-questions) is in `addrs/` and the file's git history.

## Current focus

Fresh review pass starting. jie-platform scope only. No open items; awaiting new gaps.

## Open

(none)

## Past passes (compact)

- **Fresh review pass 5 (2026-06-14):** 5 gaps closed in one pass. (1) Group 6: clarified `onUpdate` plumbing in v1 — the Jie `Tool` interface has no `onUpdate` parameter, so the pi-agent adapter discards it; v1 tools return a single final `ToolResult`. Event Bridging table gains a `tool_execution_update` row noting Day 2. (2) Group 7: removed `JieHandle.swapTeam`. The method was a redundant wrapper around `loadTeam`; the "swap" semantic is the TUI's view change, not a body-lifecycle change. TUI calls `loadTeam(teamId)` (idempotent ensure-loaded) and switches its view. ADR 15 amended (interface, decision section, amendment history); ADR 21 implications updated; `00-overview.md` glossary and `09-deployment.md` startup step 10 fixed; `addrs/17-v1-mvp-scope.md` updated. (3) Group 8: dropped "work unit created" from `09-deployment.md` "Logging" `INFO` row. The platform has no work-unit concept. (4) Group 9: confirmed — `transformContext` wrapper stays wired in v1 (it's a no-op when `CompactionSettings.enabled = false`; Day 2 ships by swapping the inner). No spec edit. (5) Group 10: added `web_search` Failure Handling subsection — transient provider failures (429, 5xx, network errors, layout changes) surface as typed tool error `web_search_failed: <message>`; no retry in v1; LLM reasons. Same pattern as bash `command_timed_out`. No new ADRs written; ADR 15 amended.
- **Fresh review pass 4 (2026-06-13):** 5 gaps closed. (1) `MemoryManager` gains `mostRecentSessionId(teamId)` and `hasSession(teamId, sessionId)`; `startJie` does the `--continue` / `--resume` resolution and validation internally via the new `StartJieOptions.continueLastSession` flag (ADR 22). The CLI no longer runs session-id SQL itself. (2) `agent.continue()` made conditional — called only when the last restored message is `user` or `toolResult`; otherwise the body waits for `agent.prompt()` from the subscription queue (per pi-agent's API contract). (3) Env-var API key resolution removed — `auth.json` is the sole credential source in v1 (ADR 23). `jie --api-key` and `jie login` are the only ways to set credentials; no `ANTHROPIC_API_KEY` etc. fallback. (4) `-p` mode now uses `handle.waitForIdle()`; the ad-hoc `currentlyBusy` Set and the team-filter inconsistency it carried are removed. (5) `waitForIdle` is the platform's idle-detection primitive; `StartJieOptions.onIdle` retained for Day-2+ use. ADRs written: 22, 23. ADR 15 amended. Full edit trail in this file's git history.
- **Fresh review pass 3 (2026-06-13):** 16 gaps closed in one pass. Headline: multi-team coexistence in v1 (Gap 3 → ADR 21) — team-scoped subject scheme, on-demand loading, swap is a view change. Plus tool error-code pattern (`read_file` / `write_file` / `write_artifact` / `notify` snake_case codes per Gap 13), CLI flag rules (Gaps 7, 12), HTTP/content rules (Gaps 5, 6, 8), EventBus error containment (Gap 4), TEAM.md validation (Gap 9), bash OS-signal handling (Gap 14), platform-topic rejection in `subscribe:` (Gap 15) and `notify` topic validation (Gap 16), artifact key/content caps (Gap 10), `write_file` content cap (Gap 11), Day 2+ `edit_file` tool entry, backlog item #22 for BLOB+compression. ADRs written: 21. Full edit trail and option choices in this file's git history.
